services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ceems-phoenix-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - ceems-network
